const myObject ={ js:'javascript',
    cpp:'c++',
    rb:'ruby',
    swift:'swift by apple'
}

for (const key in myObject) {
   //console.log(`${key} shortcut for: ${myObject[key]}`);
        
    }

    const program =["js","C++","Shell","ruby","python"]

    for (const key in program) {
        console.log(program.key);
    }
